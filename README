# 🧮 Math Expression to LaTeX Converter

This application transcribes **spoken math expressions** and converts them into **LaTeX format**. It uses **Whisper** for speech-to-text and offers two parsing options:  

- **Custom rule-based parser** – optimized for simple expressions  
- **GPT-4 powered parser** – handles more complex expressions  

---

## 🚀 Features
- 🎙️ Speech-to-text transcription with OpenAI Whisper  
- ✍️ Automatic conversion of math expressions into LaTeX  
- 🔀 Two parsing modes: rule-based or GPT-4  
- 🌐 Web interface built with Flask  

---

## 📦 Setup Instructions

### Prerequisites
- Python **3.7+**  
- `pip` (Python package manager)  
- Git (optional, for cloning)  

---

### 1️⃣ Clone the Repository
```bash
git clone https://g
